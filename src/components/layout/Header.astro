---
import Search from "@/components/Search.astro";
import ThemeToggle from "@/components/ThemeToggle.astro";
import { menuLinks } from "@/site.config";
import { withBase } from "@/utils/url";
import { siteConfig } from "../../site.config";

const currentPath =
	Astro.url.pathname.replace(import.meta.env.BASE_URL.replace(/\/$/, ""), "") || "/";
---

<header class="mb-9 flex items-center">
	<div class="flex flex-col items-start">
		<a
			aria-current={currentPath === "/" ? "page" : false}
			class="inline-flex items-center text-left"
			href={withBase("/")}
		>
			<span class="site-title-highlight text-xl font-bold sm:text-2xl">[{siteConfig.title}]</span>
		</a>
		<nav aria-label="Main menu" class="mt-1 flex flex-row flex-wrap text-left" id="navigation-menu">
			{
				menuLinks.map((link) => (
					<a
						aria-current={currentPath === link.path ? "page" : false}
						class="text-accent py-1 pe-4 font-semibold first:ps-0 sm:underline-offset-2 sm:hover:underline"
						href={withBase(link.path)}
					>
						<span class="text-gray-400 dark:text-gray-600">./</span>{link.title}
					</a>
				))
			}
		</nav>
	</div>
	<Search />
	<ThemeToggle />
</header>
